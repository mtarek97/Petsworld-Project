<!DOCTYPE html>
<html>
<head>
	<link rel="shortcut icon" href="http://www.iconj.com/ico/2/i/2ik4fcdhxx.ico" type="image/x-icon" />
	<title><%= yield(:title) %> | Pets World</title>
	<%= stylesheet_link_tag 'application', media: 'all',
	'data-turbolinks-track' => true %>
	<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
	<%= csrf_meta_tags %>
	<meta name="google-site-verification" content="Q7OooZEJrAAI_B93Jiy7IWYPWkdAW_No0OmekhVzvPU" />
</head>
<% if !logged_in? %>
<body class="body-welcome">
	<% else %>
	<body>
		<% end %>  
		<%= render 'layouts/header' if logged_in? %>
		<div class="container">
			<% flash.each do |message_type, message| %>
			<div class="alert alert-<%= message_type %>"><%= message %></div>
			<% end %>
			<%= yield %>
			<%= render 'layouts/footer' if logged_in? %>
		</div>
	</body>
	</html>
